# Zkopíruj jako deploy/.env a doplň skutečné hodnoty.
# deploy/.env NIKDY necommituj (.gitignore ignoruje .env, .env.example je výjimka).

SFTP_HOST=sftp.svethostingu.cz
SFTP_PORT=24
SFTP_USER=
SFTP_PASS=
SFTP_REMOTE_DIR=/www
